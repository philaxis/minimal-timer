# Nested tags and history QA

## Data rules

- [x] `#SNU/26-2학기/선형대수학`처럼 정규화된 경로를 만들고 빈 구간·중복 경로를 거부한다.
- [x] 상위 태그 조회는 모든 하위 경로를 포함한다.
- [x] 태그 이름 변경·병합은 현재 블록만 갱신하며 과거 세션의 태그 스냅샷은 바꾸지 않는다.
- [x] 병합 대상에 같은 하위 경로가 있으면 하나로 합치고 원본 경로는 보관 처리한다.
- [x] 기존 저장 타이머는 태그 없음·중요도 보통·종료일 없음으로 안전하게 열린다.

## Session rules

- [x] 실행 당시 블록명·태그·중요도와 시작·종료·실행 구간을 세션에 저장한다.
- [x] 단독 실행 합계가 10초 미만이면 기록하지 않는다.
- [x] 10초 미만 일시정지 뒤 재개하면 같은 세션 ID를 쓰되 중단 시간은 실행 시간에서 뺀다.
- [x] 10초 이상 일시정지 뒤 재개하면 새 세션을 시작한다.
- [x] 완료·초기화·모드 변경·블록 삭제 시 진행 중 세션을 확정한다.
- [x] 닫혀 있던 타이머가 완료됐으면 다시 열 때 정확한 완료 시각까지만 기록한다.
- [x] 같은 세션을 여러 기기가 저장해도 한 건만 남고 더 긴 실행 구간을 보존한다.

## Calendar and timeline

- [x] 월간 달력에 날짜별 블록명·실행 분량·일간 총 실행 시간과 종료 예정 블록을 표시한다.
- [x] 겹친 세션은 블록별 분량에는 각각 포함하지만 일간 총합에는 한 번만 포함한다.
- [x] 자정을 넘는 세션은 계정 시간대의 날짜 경계에서 나누어 표시한다.
- [x] 날짜 팝업의 세로 타임라인에서 겹친 세션을 나란히 표시한다.
- [x] 날짜 팝업에서 세션 하나를 삭제하면 블록과 다른 기록은 유지된다.
- [x] 태그·상위 태그·중요도 필터가 달력 합계와 타임라인에 함께 적용된다.

## UI and sync

- [x] 블록 설정에서 여러 태그, 날짜형 종료일, 낮음·보통·높음 중요도를 저장한다.
- [x] 태그 검색은 경로 구분자를 무시한 부분 문자 검색을 지원하고, 검색 전에는 최근 태그를 최대 5개만 표시한다.
- [x] 기본 마감일순·중요도순·직접 정렬과 마감일별 묶음 보기를 제공한다.
- [x] 블록 크기 조절값을 브라우저에 보존하고 모바일에서 한 행에 3개를 표시한다.
- [x] 블록명·유형·이동 손잡이를 한 행에 두고 상태 문구 없이 색과 명도로 실행 상태를 구분한다.
- [x] 설정에서 태그 추가·보관·이름 변경·병합과 계정 시간대 변경이 가능하다.
- [x] 블록 삭제 뒤에도 기록과 달력은 남는다.
- [x] 완료된 타이머는 초기화 후 다시 실행하며 실행마다 별도 세션이 생긴다.
- [x] 비로그인은 브라우저에 보존하고 Google 로그인 뒤 기존 계정 데이터와 합친다.
- [x] PC·모바일 3열 화면에서 타이머와 달력 조작에 가로 스크롤이 생기지 않는다.

## Security and recovery

- [x] 태그·세션·시간대 테이블은 RLS로 계정별 격리된다.
- [x] 브라우저 역할은 다른 사용자의 기록을 조회·수정·삭제할 수 없다.
- [x] 새 테이블은 Data API 권한을 명시적으로 부여한다.
- [x] 네트워크 저장 실패 시 로컬 데이터는 유지하고 재연결 뒤 서버 상태를 다시 읽는다.

## 실행 결과

- 2026-09-14: 단위 10개, 빌드, 데스크톱·390px 3열 브라우저, 정렬·마감별 보기·태그 검색·블록 크기 회귀 검증 통과.
- Supabase 성능 Advisor 지적 0건. 보안 경고는 제품 요구인 익명 로그인 정책과 사용하지 않는 비밀번호 로그인 보호 설정이다.
